spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
    database: postgresql
  datasource:
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: postgres
    database: postgres
    driverClassname: org.postgresql.Driver
